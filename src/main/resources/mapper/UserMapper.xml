<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yyy.sideproject.mapper.UserMapper">
	<select id="srchUser" resultType="com.yyy.sideproject.dto.UserResponse">
    SELECT ID, NAME, EMAIL, ROLE FROM USERS
    <if test="searchType != 'all' and keyword != null and keyword != ''">
	    <where>
	        <if test="searchType == 'id'">
	            ID LIKE '%' || #{keyword} || '%'
	        </if>
	        <if test="searchType == 'name'">
	            NAME LIKE '%' || #{keyword} || '%'
	        </if>
	    </where>
    </if>
	</select>

	<insert id="save" parameterType="com.yyy.sideproject.dto.UserRequest">
        INSERT INTO USERS (NAME, ID, EMAIL, PASSWORD, ROLE, CREATED_AT)
        VALUES (#{name}, #{id}, #{email}, #{password}, 'role', SYSDATE)
    </insert>
    
    <select id="loginUser" resultType="com.yyy.sideproject.dto.UserResponse">
    SELECT ID, NAME, EMAIL, ROLE 
   	  FROM USERS
     WHERE ID = #{id}
       AND PASSWORD = #{password}
	</select>
</mapper>